以下のPythonコードを、機能の内容を一切変えずに、Streamlitでそのまま実行できる完成版の app.py に整えてください。

目的：
レシート内容を入力し、Dify APIで解析した結果を確認・修正して在庫登録し、その在庫と家族条件・避ける食材・健康目標・調理時間をもとに、Dify APIで献立を1つ提案するアプリを作成します。

絶対条件：
・機能の内容は変更しないでください。
・画面構成、処理の流れ、変数名、関数名、session_state のキーは基本的に維持してください。
・新しい機能を勝手に追加しないでください。
・既存のロジックを削除しないでください。
・コード中に含まれる <br> はすべて削除し、Pythonとして実行可能な形にしてください。
・Streamlitで `streamlit run app.py` として実行できる1つのPythonファイルにしてください。
・インデント、改行、空行を整えて読みやすくしてください。
・構文エラーが出ないようにしてください。
・日本語の表示文言は変更しないでください。
・Dify APIの呼び出し処理は維持してください。
・st.secrets のキー名は以下のまま使ってください。
  - DIFY_API_URL
  - DIFY_API_KEY
  - RECIPE_API_KEY
・APIキーなどの具体値はコードに直接書かないでください。
・HTMLタグを表示する必要はありません。
・pandas、requests、streamlit、re、以外の外部ライブラリは追加しないでください。

このアプリに含める機能：
1. レシート内容をテキストエリアに入力する
2. Dify APIでレシートを解析する
3. AI解析結果を表示する
4. 解析結果を在庫登録候補として表形式で表示する
5. ユーザーが登録判定、食材名、数量、単位、カテゴリ、確認状態を修正できる
6. 登録判定が「登録する」の食材だけを在庫に登録する
7. 現在の在庫一覧を表示する
8. 在庫一覧を編集・削除できる
9. 直前の在庫登録を取り消せる
10. 家族条件、避ける食材・避ける条件、健康目標、調理時間を入力できる
11. 条件に合わない在庫食材を献立提案から除外する
12. Dify APIで献立を1つ提案する
13. 使用禁止食材が提案結果に含まれていた場合は、再提案する
14. それでも使用禁止食材が含まれていた場合はエラー表示する
15. 献立を作った後に、在庫を編集・削除して更新できる

注意点：
・レシート解析用APIキーには `st.secrets["DIFY_API_KEY"]` を使ってください。
・献立提案用APIキーには `st.secrets["RECIPE_API_KEY"]` を使ってください。
・Dify API URLには `st.secrets["DIFY_API_URL"]` を使ってください。
・`call_dify()`、`get_answer()`、`strip_html()`、`parse_answer()`、`split_inventory_for_recipe()`、`build_recipe_query()` などの関数は維持してください。
・`CATEGORY_OPTIONS`、`UNIT_OPTIONS`、`SPICY_KEYWORDS`、`BITTER_KEYWORDS`、`HARD_KEYWORDS`、`FATTY_KEYWORDS` の内容は変更しないでください。
・出力は完成版のPythonコードのみでお願いします。
・説明文や補足は不要です。
・コードブロック形式で出力してください。

以下が元コードです。

【ここに元コードを貼り付ける】
